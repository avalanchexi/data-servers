# 数据服务平台原型设计规范

> 版本：1.1（协作评审基线）
>
> 更新日期：2026-08-28
>
> 适用范围：数据服务平台桌面 Web 原型
>
> 视觉基准：[合并原型](prototypes/data-service-platform-prototype.html)

## 1. 文档目的

本规范用于统一多人协作时的数据服务平台原型风格。新增页面、修改页面和后续合并内容都必须复用同一套栅格、字体、颜色、布局与组件规则，不得因来源页面不同而建立平行样式。

本文中的规范词含义如下：

- **必须**：合并验收的硬性要求。
- **应该**：默认采用；只有明确的业务或交互原因才能偏离。
- **可以**：经过说明后允许使用的变体。

## 2. 规范来源与冲突处理

规范优先级从高到低如下：

1. [企业原型设计规范 Skill](.agents/skills/enterprise-prototype-design/SKILL.md) 与 [设计 tokens](.agents/skills/enterprise-prototype-design/assets/design-tokens.css)：视觉与交互的唯一来源。
2. [领域文档](CONTEXT.md)：产品术语、对象边界、状态名称和动作语义的唯一来源。
3. 本文档：面向协作的执行摘要与组件选型规则。
4. [合并原型](prototypes/data-service-platform-prototype.html)：页面结构、实际效果和交互参考。

若现有原型中的局部样式与本文不一致，应视为待收敛的遗留实现，不应复制到新页面。禁止新增另一套颜色、字号、间距、圆角或组件命名体系。

## 3. 已确认的协作基线

所有协作者应先确认以下设计决策：

- 只设计桌面 Web，以 `1440 × 900` 为主要视口，最小支持宽度为 `1200px`。
- 产品壳层固定使用 `60px` 顶栏和 `220px` 左侧导航。
- 页面默认字号为 `14px`，页面标题为 `24px`，不使用任意字号。
- 页面使用 12 栏栅格，栏间距为 `16px`，页面边距为 `24px`。
- 一级导航、业务状态、元数据 Tag、计数徽标分别使用固定组件，不因模块不同而改变。
- 新增、编辑和修改使用弹窗；简短只读详情使用抽屉；信息量大或包含连续任务时使用详情页。
- 所有颜色、尺寸和动效必须引用 `--ds-*` tokens。
- 相同语义只能有一种组件结构和一套状态样式；页面来源类名不是新增组件的理由。

## 4. 设计原则

### 4.1 品牌中立

- 不使用第三方 Logo、品牌名称、宣传语、远程字体或远程图片。
- 图标使用风格统一的内联 SVG；装饰图标设置 `aria-hidden="true"`。
- 产品名称使用“数据服务平台”。顶部平台模块当前只展示“数据服务”；“数据接入”“数据开发”“数据治理”等模块后续按同一组件扩展。

### 4.2 Tokens 优先

- CSS 必须使用 `--ds-*` 语义变量。
- 禁止在组件样式中直接增加十六进制颜色。
- 禁止通过页面级覆盖改变已有 token 的值。
- 本文只列出常用 tokens；完整值以 `design-tokens.css` 为准。

### 4.3 克制的信息层级

- 画布和主要容器只形成两层背景。
- 默认卡片不使用阴影；阴影只表达浮层或可拖拽、可悬浮层级。
- 同一任务区域只保留一个主操作。
- 页面优先显示当前任务需要的信息，不用装饰性大标题和无业务意义的指标。

## 5. 栅格与尺寸

### 5.1 桌面栅格

| 项目 | Token / 数值 | 使用规则 |
| --- | --- | --- |
| 主验收视口 | `1440 × 900` | 所有原型必须在此视口完成视觉验收 |
| 最小桌面宽度 | `--ds-desktop-min` / `1200px` | 小于该宽度不设计移动端重排 |
| 栅格列数 | 12 栏 | 页面主体按 12 栏组合 |
| 栏间距 | `--ds-grid-gutter` / `16px` | 卡片、面板和列之间统一使用 |
| 页面边距 | `--ds-page-padding` / `24px` | 页面头部和内容主体左右统一 |
| 内容最大宽度 | `--ds-content-max` / `1440px` | 内容居中，不无限拉伸 |
| 顶栏高度 | `--ds-nav-height` / `60px` | 全模块保持一致 |
| 侧栏宽度 | `--ds-sidebar-width` / `220px` | 全模块保持一致 |

### 5.2 间距体系

只使用以下间距 tokens：

| Token | 数值 | 常见用途 |
| --- | ---: | --- |
| `--ds-space-2` | 4px | 导航项间距、紧邻元素 |
| `--ds-space-3` | 6px | 图标与状态文字、紧凑内间距 |
| `--ds-space-4` | 8px | 图标与文字、轻量分组 |
| `--ds-space-5` | 10px | Tag 横向内边距 |
| `--ds-space-6` | 12px | 控件内边距、导航内容间距 |
| `--ds-space-7` | 16px | 卡片内边距、区块基础间距 |
| `--ds-space-8` | 20px | 表单项、弹层内容间距 |
| `--ds-space-9` | 24px | 页面级内边距、区块间距 |
| `--ds-space-10` | 32px | 大区块分隔 |
| `--ds-space-13` | 48px | 空状态等展示区留白 |

禁止使用 `13px`、`18px`、`22px` 等任意间距代替已有 tokens。组件协议明确规定的高度或细节尺寸除外。

## 6. 页面布局

### 6.1 产品壳层

```text
┌─────────────────────────────────────────────────────────┐
│ 顶栏 60px：产品标识｜平台模块 Menu｜帮助、通知、角色   │
├───────────────┬─────────────────────────────────────────┤
│ 左侧导航      │ 页面头部：面包屑、标题、说明、主操作    │
│ 220px         ├─────────────────────────────────────────┤
│               │ 页面内容：24px 边距，12 栏栅格          │
└───────────────┴─────────────────────────────────────────┘
```

- 顶栏固定在视口顶部，层级使用 `z-index: 30`。
- 顶栏平台模块使用横向 Menu。当前只展示并选中“数据服务”，点击后进入数据服务默认入口“服务集市”。
- 未上线的平台模块不显示禁用占位；后续“数据接入”“数据开发”“数据治理”等模块接入时复用同一 Menu Item。
- 左侧导航固定在顶栏下方，层级使用 `z-index: 20`，只允许侧栏内部纵向滚动。
- 工作区从 `top: 60px`、`left: 220px` 开始，页面滚动只发生在工作区内。
- 页面整体不得产生横向滚动。

### 6.2 顶部平台模块 Menu

顶部 Menu 参考 [Arco Design Menu 规范](https://arco.design/docs/spec/menu) 的横向导航模式，用于切换平台级业务模块，不承载数据服务域内页面。

| 元素 | 规格 |
| --- | --- |
| Menu 容器 | 高 `60px`，紧随产品标识，横向排列 |
| Menu Item | 最小宽度 `88px`，水平内边距 `20px`，字号 `14px` |
| 默认态 | `--ds-text-2` 文字、透明背景、字重 `400` |
| Hover 态 | `--ds-text-1` 文字、`gray-1` 背景 |
| 选中态 | `primary-6` 文字、字重 `500`、底部 `2px` 主色指示条 |
| 焦点态 | 使用 `--ds-focus-ring` 的内嵌焦点环 |

- 顶部 Menu 必须使用 `.ds-top-menu`、`.ds-top-menu-list` 和 `.ds-top-menu-item`，不得继续使用 `.top-domain` 创建平行实现。
- Menu Item 使用可导航链接并提供当前项语义；当前“数据服务”使用 `aria-current="page"`。
- 平台模块顺序在完整信息架构确认后统一维护，不允许协作页面自行插入或调整顺序。
- 顶部平台模块与左侧域内目录不得使用相同层级或重复同一批入口。

### 6.3 左侧主导航

目录顺序固定为：

1. 服务集市
2. 数据应用
3. 服务开发
4. 审批中心
5. 运行管理
6. 设置

默认入口为“服务集市”。“服务开发”和“运行管理”可以展开三级目录；“设置”前使用分隔线。

| 元素 | 规格 |
| --- | --- |
| 一级导航项 | 高 `36px`，项间距 `4px`，左右内边距 `12px`，圆角 `2px` |
| 一级图标 | `18 × 18px`，图标与文字间距 `12px` |
| 二级导航项 | 高 `32px`，左内边距 `20px`，左右内容内边距 `12px` |
| 展开区 | 上下内边距 `6px`，左缩进 `20px` |
| 选中态 | `primary-1` 背景、`primary-7` 文字、字重 `500` |
| 二级选中标记 | 左侧 `3 × 16px` 主色竖条 |
| 展开箭头 | `16 × 16px`，`200ms` 旋转；减少动效时立即切换 |

主导航统一使用 `.ds-main-nav`、`.ds-nav-item`、`.ds-nav-parent`、`.ds-nav-children` 和 `.ds-nav-child`。不得用 `.nav-item`、`.nav-group-button` 等来源类名定义另一套尺寸或状态。

### 6.4 页面头部

- 页面头部使用白色表面背景和底部分隔线。
- 内边距为 `16px 24px`。
- 信息顺序为：面包屑 → 页面标题 → 页面说明。
- 页面级主操作放在右侧，与标题区底部对齐。
- 页面只允许一个 `h1`。

### 6.5 页面内容

- 页面内容左右内边距为 `24px`，大区块之间优先使用 `24px` 或 `32px`。
- 摘要指标位于筛选与主数据之前。
- 筛选、局部操作位于表格或卡片列表上方。
- 宽表格放入 `.table-scroll`，只允许表格容器横向滚动。

## 7. 字体与字号

### 7.1 字体族

正文使用系统字体回退，不请求远程字体：

```css
font-family: var(--ds-font-sans);
```

中文优先使用系统可用的苹方、思源黑体或微软雅黑；代码、端口、路径和固定标识使用 `--ds-font-mono`。

### 7.2 字号层级

| 用途 | 字号 | 字重 | 行高 |
| --- | ---: | ---: | ---: |
| 辅助信息、面包屑、Tag | `12px` | `400`，状态为 `500` | `1.5715` |
| 正文、控件、表格、三级标题 | `14px` | `400` 或 `500` | `1.5715` |
| 模块标题、卡片标题、弹窗标题 | `16px` | `500` | `1.35` |
| 次级页面标题 | `20px` | `500` | `1.35` |
| 页面标题、常规强调数字 | `24px` | `500` | `1.35` |
| 大型核心指标 | `36px` | `500` | `1.35` |

禁止使用 `13px`、`15px`、`18px`、`22px`、`28px` 等平行字号。不要用 `700` 或更重字重堆叠层级。

### 7.3 文字颜色

- 正文和标题：`--ds-text-1`。
- 次级文字、普通导航：`--ds-text-2`。
- 辅助说明、时间、占位信息：`--ds-text-3`。
- 禁用或弱化信息：`--ds-text-4`，且不能只靠颜色表达不可用。

## 8. 配色

### 8.1 基础语义色

| 语义 | Token | 色值 | 用途 |
| --- | --- | --- | --- |
| 主色 | `--ds-color-primary-6` | `#165dff` | 主按钮、链接、焦点、图表主序列 |
| 深主色 | `--ds-color-primary-7` | `#0e42d2` | 选中文字、主按钮悬停 |
| 浅主色 | `--ds-color-primary-1` | `#e8f3ff` | 选中背景、信息 Tag |
| 页面画布 | `--ds-bg-canvas` | `#f2f3f5` | 工作区背景 |
| 主要表面 | `--ds-bg-surface` | `#ffffff` | 卡片、表格、弹层 |
| 填充背景 | `--ds-bg-fill` | `#f2f3f5` | 中性 Tag、弱选项 |
| 悬停填充 | `--ds-bg-fill-hover` | `#e5e6eb` | 非主操作悬停 |
| 主文字 | `--ds-text-1` | `#1d2129` | 标题、正文 |
| 次文字 | `--ds-text-2` | `#4e5969` | 次要正文、导航 |
| 辅助文字 | `--ds-text-3` | `#86909c` | 说明、时间、占位 |
| 普通边框 | `--ds-border-2` | `#e5e6eb` | 卡片、表格分隔 |
| 控件边框 | `--ds-border-3` | `#c9cdd4` | 输入框、按钮 |

### 8.2 状态语义色

| 状态语义 | 主色 | 浅背景 | 文字色 |
| --- | --- | --- | --- |
| 成功、正常、可用 | `#00b42a` | `#e8ffea` | `#087a24` |
| 警告、待处理、降级 | `#ff7d00` | `#fff7e8` | `#a84b00` |
| 危险、失败、异常 | `#f53f3f` | `#ffece8` | `#cb2634` |
| 信息、验证中 | `--ds-color-primary-6` | `--ds-color-primary-1` | `--ds-color-primary-7` |
| 中性、停止、隐藏 | `--ds-text-2` | `--ds-bg-fill` | `--ds-text-2` |

状态必须同时显示文字和圆点或图标，不能只靠颜色。状态名称必须沿用 `CONTEXT.md`，视觉色不改变业务状态语义。

### 8.3 图表颜色

- 主序列优先使用 `primary-6`，正常/成功序列使用成功色。
- 同一指标在不同页面保持相同颜色。
- 不使用状态危险色表达普通对比数据。
- 图例必须有文字，不只使用颜色区分。

## 9. 圆角、边框、阴影与动效

| 类型 | Token / 数值 | 使用范围 |
| --- | --- | --- |
| 小圆角 | `--ds-radius-small` / `2px` | 按钮、输入框、导航项、Tag |
| 中圆角 | `--ds-radius-medium` / `4px` | 卡片、表格容器、上下文 Tag |
| 大圆角 | `--ds-radius-large` / `8px` | 弹窗、大型展示容器 |
| 普通边框 | `1px solid var(--ds-border-2)` | 卡片、表格、分组 |
| 控件边框 | `1px solid var(--ds-border-3)` | 按钮、输入框 |
| 普通浮层 | `--ds-shadow-1` | Popover、可悬浮卡片 |
| 弹层 | `--ds-shadow-3` | 弹窗、抽屉 |

动效只用于状态变化：

- 即时反馈：`100ms`。
- 展开、选中和普通浮层：`200ms`。
- 复杂进入：最多 `300ms`。
- `prefers-reduced-motion: reduce` 下持续时间必须归零。

## 10. 组件规范

### 10.1 组件复用原则

同一语义必须选择同一组件。判断顺序如下：

| 用户需要理解的内容 | 使用组件 | 不应使用 |
| --- | --- | --- |
| “当前处于什么状态” | Status | 普通 Tag、仅颜色文字 |
| “这是什么类型、版本或上下文” | Tag | Status |
| “有多少项” | Count Badge | Status、Tag |
| “点击后执行动作” | Button | 可点击 `div`、静态 Tag |
| “点击后前往另一位置” | Link | Button 模拟链接 |

来源页面的 `.environment-chip`、`.manager-chip`、`.type-badge` 等类名只能视为遗留别名；新内容统一映射到本节定义的 Tag 变体。

### 10.2 按钮

| 变体 | 规格 | 使用场景 |
| --- | --- | --- |
| 默认按钮 | 高 `32px`，左右内边距 `15px`，圆角 `2px` | 次要动作、取消、关闭 |
| 主按钮 | 主色背景、白色文字 | 当前任务区唯一主操作 |
| 弱强调按钮 | 透明或白色背景、灰色边框、主色文字 | 查看、继续配置等局部动作 |
| 危险按钮 | 白色背景、危险色文字；悬停可使用危险色填充 | 删除、紧急停流等风险动作 |
| 小按钮 | 高 `28px`，左右内边距 `10px`，字号 `12px` | 表格和卡片行内操作 |
| 图标按钮 | `32 × 32px`，圆角 `2px` | 关闭、帮助、搜索等单图标动作 |
| 顶栏图标按钮 | `36 × 36px`，圆角 `4px` | 顶栏帮助、通知 |

规则：

- 按钮使用 `.btn`，通过 `.btn-primary`、`.btn-danger`、`.btn-small` 等变体扩展。
- 单图标按钮统一使用 `.icon-btn`，必须提供 `aria-label`。
- 禁止创建无类名的 `20px` 帮助按钮；桌面主要交互目标至少为 `32px`。
- 加载中保持原宽、阻止重复提交，并显示明确反馈。
- 危险操作必须在二次确认中说明对象、影响和恢复方式。

### 10.3 Status 状态标签

Status 只表达业务或运行状态：

- 高度 `22px`。
- 字号 `12px`、字重 `500`。
- 左右内边距 `10px`，圆角 `2px`。
- 状态文字前使用 `6 × 6px` 同色圆点。
- 使用 `.status` 加 `.status-success`、`.status-warning`、`.status-danger`、`.status-info` 或 `.status-neutral`。

相同状态在任何模块中必须保持同一颜色和结构。例如“正常”和“可用”使用成功样式，“降级”和“待审核”使用警告样式，“异常”和“失败”使用危险样式。

### 10.4 Tag 元数据标签

Tag 用于类型、版本、身份、范围和上下文，不表达业务状态：

| 变体 | 规格 |
| --- | --- |
| 默认 Tag | 高 `22px`，字号 `12px`、字重 `400`，左右内边距 `10px`，灰色边框、白色背景 |
| 主色 Tag | 高 `22px`，浅主色背景、深主色文字、浅主色边框 |
| 中性上下文 Tag | 高 `28px`，圆角 `4px`，灰色填充或边框 |

统一使用 `.tag` 和明确变体。禁止让同名 `.tag` 在不同页面出现 `22px` 与 `27px` 两种高度。

### 10.5 Count Badge 计数徽标

- 只显示数量，不承载业务状态。
- 最小宽度 `24px`，视觉高度 `20px`，字号 `12px`。
- 使用中性填充、辅助文字和胶囊圆角。
- 数量超过展示上限时使用 `99+` 等明确缩写。

### 10.6 输入框、选择器和表单

| 控件 | 规格 |
| --- | --- |
| Input / Select | 高 `32px`，左右内边距 `12px`，圆角 `2px` |
| Textarea | 默认最小高度 `92px`；弹窗内短说明可以使用紧凑 `66px` 变体 |
| 字段间距 | `--ds-form-item-gap` / `20px` |
| 标签 | 可见、位于控件上方；同一表单不混用左右标签布局 |
| 错误 | 紧贴字段，说明修复方式，并通过 `aria-describedby` 关联 |

- 占位符只提供示例，不能替代字段标签。
- 禁用控件使用填充背景，同时保留可读文字。
- 选项超过 8 项时提供搜索或更合适的选择模式。
- 表单末尾放置取消与提交操作，主操作位于最右侧。

### 10.7 表格与分页

- 表头使用 `14px / 500`，背景为 `--ds-color-gray-1`。
- 表头和单元格内边距统一为 `9px 16px`。
- 表格使用 `caption`、`thead`、`tbody` 和带 `scope` 的表头。
- 空值显示“—”，不留空字符串。
- 状态列使用 Status，类型和版本使用 Tag。
- 操作列保持可识别，不为塞入更多列过度压缩。
- 分页紧随表格，显示总量和当前位置；筛选变化后返回第一页。

### 10.8 卡片与面板

- 默认使用白色表面、`1px` 普通边框和 `4px` 圆角。
- 常规内边距为 `16px`，信息密集区域可以使用 `20px` 或 `24px`。
- 摘要卡高度和标题位置必须在同一组内一致。
- 默认不加阴影；只有可拖拽卡片、悬浮卡片或浮层使用 `--ds-shadow-1`。
- 卡片中的主操作不得与页面主操作竞争视觉层级。

### 10.9 Tabs、筛选器与目录

- Tabs 只切换同一上下文中的并列内容。
- 类型筛选按钮用于过滤集合，不使用 `role="tab"` 伪装 Tabs。
- 左侧目录用于切换不同配置对象或实例，不替代主导航。
- 选中态统一使用浅主色背景、深主色文字；同层级不同时出现两种选中表达。
- 步骤条只表示有顺序的任务进度。

### 10.10 弹窗、抽屉和详情页

| 载体 | 使用场景 | 规格 |
| --- | --- | --- |
| 标准弹窗 | 新增、编辑、修改、短任务 | 宽 `520px`，圆角 `8px`，`--ds-shadow-3` |
| 宽弹窗 | 字段较多但仍可一次完成的编辑 | 宽 `720px`，最大高度为视口减 `80px` |
| 右侧抽屉 | 简短只读详情、影响预览 | 宽 `min(560px, 48vw)`，全高，`--ds-shadow-3` |
| 独立详情页 | 三个以上独立信息区、表格或历史记录、连续任务 | 使用页面头部和标准内容布局 |

硬性规则：

- 新增和修改不得使用抽屉。
- 只读和详情可以使用抽屉。
- 内容过多、需要深链接、包含多段连续操作时使用详情页。
- 弹窗和抽屉头部最小高度为 `60px`，标题为 `16px / 500`。
- 内容区内边距为 `20px`，底部操作区使用顶部边框并右对齐。
- 关闭按钮统一为 `32 × 32px` 图标按钮；不得分别建立 `.close-button` 和 `.close-btn` 两套视觉协议。

### 10.11 Alert、Toast 与反馈

- 字段问题使用字段错误，页面内问题使用 Alert，跨区域操作结果使用 Toast。
- Toast 位于工作区顶部居中，不相对整个浏览器居中。
- Toast 最大宽度为 `520px`，使用 `role="status"`；紧急错误才使用 `role="alert"`。
- 成功反馈说明完成了哪个对象和下一步，不只重复按钮文字。
- 错误反馈说明原因、影响和恢复动作。

### 10.12 空状态与加载状态

- 空状态说明为什么为空，并给出一个推荐动作。
- 无权限不能伪装成空数据，必须说明缺少的资格和获取路径。
- 加载状态保持原布局尺寸，避免内容跳动。
- 不使用无法停止的循环动画。

## 11. 内容与术语

- 页面名称、对象名称、状态和动作必须使用 `CONTEXT.md` 中的正式术语。
- 页面标题使用名词或任务名，例如“运行管理”“创建 AI 服务”。
- 按钮使用“动词 + 对象”，例如“创建服务”“提交审核”。
- 风险动作必须明确对象，例如“停用 Agent 对外呈现”，不只写“确认”。
- 示例数据使用真实、简洁的中文业务内容，不使用占位英文或玩笑数据。
- 未知值统一显示“—”。

## 12. 交互与可访问性

- 所有动作使用原生 `button`、`a`、`input`、`select` 或 `textarea`。
- 保留清晰的 `:focus-visible`，焦点环使用 `--ds-focus-ring`。
- DOM 顺序必须与视觉顺序一致。
- 图标按钮必须有可访问名称，装饰 SVG 必须隐藏于辅助技术。
- 正文与背景对比度至少为 `4.5:1`，大文本至少为 `3:1`。
- 表单控件与标签、帮助、错误建立程序化关联。
- 状态不能只靠颜色表达。

## 13. 桌面适配与溢出

- 必须在 `1440 × 900` 和 `1200 × 900` 下检查页面。
- 支持范围内不得出现页面整体横向滚动。
- 宽表格只在 `.table-scroll` 内横向滚动。
- 长文本需要限定宽度、换行、截断或提供完整查看路径。
- 本规范不要求移动端导航、触控布局或卡片纵向重排。

## 14. 现有原型兼容说明

合并原型已经统一了主要视觉效果，但仍存在少量遗留类名。协作开发时不得继续复制：

- 主导航父项同时存在 `.nav-item` 和 `.nav-group-button` 来源类名；规范组件只认 `.ds-nav-*`。
- 元数据仍存在 `.tag`、`.type-badge`、`.environment-chip` 和 `.manager-chip` 等别名；新内容应统一为 Tag 变体。
- 关闭按钮仍存在 `.close-button` 和 `.close-btn`；新内容统一使用图标按钮协议。
- 占位页同名 `.tag` 曾出现 `27px` 高度；规范高度固定为 `22px`。
- 服务开发的个别帮助按钮曾使用 `20px` 无类名按钮；规范交互尺寸固定为至少 `32px`。

这些兼容项是后续收敛目标，不构成允许新增的变体。

## 15. 协作交付检查清单

提交新原型或合并模块前，作者和评审者共同确认：

### 15.1 视觉

- [ ] 只使用完整的 `--ds-*` tokens，没有平行颜色和字号。
- [ ] 顶栏为 `60px`、侧栏为 `220px`、页面边距为 `24px`。
- [ ] 顶部平台模块使用统一横向 Menu，当前只展示并选中“数据服务”。
- [ ] 默认正文为 `14px`，标题层级符合本文。
- [ ] 同类按钮、Status、Tag、输入框、表格和浮层尺寸一致。
- [ ] 页面在 `1440 × 900` 和 `1200 × 900` 下无整体横向溢出。

### 15.2 组件

- [ ] 相同语义没有创建新的平行组件或类名。
- [ ] 状态使用 Status，类型、版本和上下文使用 Tag，数量使用 Count Badge。
- [ ] 同一任务区域只有一个主按钮。
- [ ] 新增和修改使用弹窗，只读详情使用抽屉或详情页。
- [ ] 图标按钮至少 `32 × 32px` 且有 `aria-label`。

### 15.3 内容与交互

- [ ] 术语和状态名称符合 `CONTEXT.md`。
- [ ] 导航、筛选、主要操作、弹窗和关闭路径可真实操作。
- [ ] 空值显示“—”，错误说明恢复动作，风险操作说明影响。
- [ ] 控制台无错误，无远程资源、第三方品牌、emoji 或个人绝对路径。
- [ ] 支持 `prefers-reduced-motion`。

## 16. 变更管理

- 新组件只有在现有组件无法组合表达需求时才能增加。
- 增加前必须说明使用场景、与现有组件的差异、尺寸、变体、状态和可访问性。
- 若多个页面需要同一新规则，应先更新本规范和设计 tokens，再更新原型。
- 不允许先在单个页面实现局部规范，再依赖合并阶段覆盖修正。
- 视觉变更至少提供 `1440 × 900` 和 `1200 × 900` 的对照检查结果。

## 17. 自动化与参考文件

- 合并原型：[prototypes/data-service-platform-prototype.html](prototypes/data-service-platform-prototype.html)
- 设计 tokens：[design-tokens.css](.agents/skills/enterprise-prototype-design/assets/design-tokens.css)
- 原型设计规范：[enterprise-prototype-design](.agents/skills/enterprise-prototype-design/SKILL.md)
- 领域术语：[CONTEXT.md](CONTEXT.md)
- 设计系统契约测试：[prototype-design-system.test.cjs](prototypes/tests/prototype-design-system.test.cjs)

如本规范与上述唯一来源发生冲突，应先修正文档，不通过页面级 CSS 覆盖掩盖冲突。

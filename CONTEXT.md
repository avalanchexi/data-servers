# 数据服务平台

本上下文描述一个面向独立建设的内外网系统，以受治理的方式提供数据访问能力，并管理服务生产与消费关系的平台。

## Language

**租户**：
数据、权限、资源和配置彼此隔离的最高所有权边界；一个部署实例可以按业务需求承载一个或多个租户。
_Avoid_：数据消费者、数据应用、业务域

**数据服务平台**：
本次重新设计的产品整体，负责承载数据服务及其消费关系。
_Avoid_：数据中台（范围过宽）、数据应用（具体消费场景）

**数据服务域**：
完整平台中的一级产品域，与数据治理、数据安全等一级产品域并列；域内包含工作台、服务集市、数据应用、应用与授权、审批中心、运行管理和设置七项功能。
_Avoid_：数据服务（指单个可消费产品）、数据服务平台（指本次重新设计的产品整体）

**数据服务域设置**：
由管理者维护、服务于数据服务域各业务功能的必要共用配置；当前包含集市产品类型、认证与密钥策略、类型专属限制三个目录项，不承载系统管理、基础设施运维或单个数据服务的配置。
_Avoid_：系统设置、运维设置、单个数据服务设置

**数据服务**：
面向外部系统发布、具有明确数据契约和管理责任的可消费数据产品；当前对外服务形态包括 API 服务和数据流服务。
_Avoid_：接口（只描述技术形态）、数据应用

**服务集市**：
每个租户唯一的统一展示、检索和申请平台可消费产品的入口；集市产品类型包括 Agent、MCP、模型、Skill、Worker、API、数据流和逻辑表，并根据产品类型、业务域、角色及权限动态过滤内容。内外网访问差异由环境和统一网关策略处理，不拆分为多个独立集市。
_Avoid_：仅包含 API 的接口市场、多个独立服务集市、服务授权

**集市产品类型**：
服务集市中用于区分产品能力形态并决定相应配置结构的分类；当前包括 Agent、MCP、模型、Skill、Worker、API、数据流和逻辑表，其中逻辑表是不可直接调用的资产类型。类型的运行语义和配置结构由平台提供，管理者只能维护展示名称、说明、排序和对外呈现状态；停用不影响既有产品、授权和调用。
_Avoid_：数据应用类型、外部系统类型、消费场景类型

**可调用服务产品**：
在服务集市中提供调用、装配或任务执行能力，并统一采用上线、下线生命周期的对象；当前包括 Agent、MCP、模型、Skill、Worker、API 和数据流。
_Avoid_：逻辑表、仅可见但不可调用的资产

**平台原生产品**：
由开发者在本平台服务开发平面内创建、经独立上线后可供数据应用选择的服务产品；首期原生开发范围包括 API、Agent、MCP、Skill 和 Worker，模型不属于本次原生开发范围。
_Avoid_：外部数据消费方、数据应用、逻辑表

**产品可见与复用范围**：
服务产品采用私有或共有范围；私有产品只允许产品负责人、平台人员和明确授权的数据应用发现，共有产品经审核后允许当前租户内符合角色和业务域条件的主体申请复用。数据应用必须固定具体产品版本，并使用应用独立的权限、凭据、配额和运行观测。
_Avoid_：外部数据消费方共享、服务授权、未经审核公开、跨租户共有

**服务创建**：
开发者根据业务需要在服务开发平面内创建平台原生产品的过程；AI 服务入口同时支持非托管数据需求登记，但该登记不产生平台可部署制品。它与管理者创建数据应用以映射外部数据消费方是不同的生产和治理路径。
_Avoid_：数据应用注册、服务使用申请、服务上线

**服务开发**：
独立于数据应用的服务生产平面，承载平台原生产品的创建、修改、测试、版本和发版准备；统一入口下区分 API 开发与 AI 服务开发，AI 服务入口可附带非托管服务的数据需求登记，不负责其外部实现或部署。
_Avoid_：数据应用开发、服务集市、运行管理

**AI 服务开发**：
服务开发中面向 Agent、MCP、Skill 和 Worker 的入口。所有类型均配置数据需求；创建时选择是否由平台托管。托管服务拥有类型专属开发内容，并共享人工审查、版本和上线治理边界；非托管服务仅登记和审核数据需求，不在平台开发、测试或部署。模型通过 AI 网关注册管理，不进入首期 AI 服务原生开发。
_Avoid_：API 开发、数据应用注册、数据应用装配

**AI 服务托管方式**：
创建 AI 服务时声明是否在平台开发和托管。选择“是”进入数据需求与类型专属开发；选择“否”仅进入数据需求选择、配置和审核。非托管需求记录不是外部能力产品，不产生可部署服务版本，也不能替代外部消费方的数据应用注册。
_Avoid_：数据权限开关、数据应用运行方式、外部地址登记

**服务数据需求**：
服务对数据资产、字段、行范围和使用用途的声明，随托管服务版本或非托管需求修订提交审核；不使用平台数据时可显式声明。需求通过只确认该声明，不授予数据访问权；运行时仍须使用数据应用独立获批的身份与数据权限。
_Avoid_：服务数据授权、开发者直连生产数据、审核即开通

**API 服务**：
以请求—响应方式向数据应用交付 ADS 层数据资产的数据服务。
_Avoid_：普通接口、数据流服务

**数据流服务**：
以持续订阅方式向数据应用交付受治理流式数据资产的数据服务；暂不以 ODS、DWD、DWS 或 ADS 等数仓语义层级作为来源准入限制，但来源必须完成租户与业务域归属、资产登记、数据权限审核和服务上线审核。数据应用必须单独申请数据权限，审批后取得订阅配置和凭证。
_Avoid_：只允许 ADS 流、任意未治理流式来源、批量 ETL、CDC 服务、API 服务

**流式数据资产**：
以持续产生或变化的记录表达业务事实或状态变化的受治理数据资产；可以属于 ODS、DWD、DWS 或 ADS 等任意语义层，层级只作为资产语义和审批信息，不作为数据流服务的硬性准入条件。只有完成归属、结构契约、质量、安全和数据权限治理的流式数据资产才可被数据流服务引用。
_Avoid_：ADS 流式资产、任意未治理数据流、数据流服务

**逻辑表**：
以业务语义组织、由 ADS 层数据资产映射形成并在服务集市中可见的数据资产；与物理表类似，可由具备开发者数据权限的数据服务开发者选取并用于构建 API 服务或数据流服务，当前不直接向数据应用提供 JDBC、SQL 或 HTTP 查询。
_Avoid_：对外逻辑表服务、物理源表直连、ODS 表

**模型服务**：
将 MaaS 已有模型通过 AI 网关注册并纳入合法合规治理后提供推理访问的模型能力；首期不在数据服务平台原生开发或训练模型，本次服务开发与注册改造不继续展开模型的开发、上线和集市生命周期。
_Avoid_：MaaS 中台、Agent

**MCP 服务**：
遵循 Model Context Protocol 暴露工具或资源能力、可被 Agent 或数据应用发现和调用的集市产品；平台原生开发必须形成符合 MCP 协议的可托管服务制品，外部 Agent 使用涉及平台数据获取、对外数据通道或中台实质操作的 MCP 前，该 MCP 必须在 AI 网关注册并取得使用资格。
_Avoid_：普通 API 服务、Skill

**受管 MCP**：
已经在 AI 网关完成注册并进入平台治理范围的 MCP 服务；涉及平台数据获取、对外数据通道或中台实质操作时，成为受管 MCP 是外部 Agent 合法装配和调用该能力的前提。受管 MCP 自身不持有数据权限，调用时使用外部 Agent 所映射数据应用的透传身份和已获授权范围。
_Avoid_：未注册 MCP、服务授权、仅填写外部地址

**Skill**：
可被 Agent 发现、装配和使用的版本化能力包，是服务集市中的 AI 类产品；交付外部 Agent 时使用签名的固定版本文件，文件不得包含平台凭据，其中的脚本通过运行时注入的数据应用身份消费数据或执行获准操作。平台只保证交付文件的来源、版本和签名，不证明外部环境实际执行的代码未被修改。
_Avoid_：MCP 服务、Worker

**Worker**：
执行异步或长时任务、具有任务输入输出和失败恢复语义的可部署运行单元，是服务集市中的 AI 类产品；原生开发形成由平台托管的独立执行制品，不与 Agent 共用同一种运行模型。首期支持由 Agent 或 API 按需提交异步任务以及平台定时触发，事件流触发后续扩展。
_Avoid_：人类开发者、Agent、数据流服务

**服务集市可见性**：
用户发现数据服务元数据的资格，由租户、业务域和角色共同约束，不代表拥有申请或调用资格。
_Avoid_：服务授权、数据权限

**服务订阅申请**：
数据应用负责人请求获得某项数据服务访问权的业务申请。
_Avoid_：服务授权

**服务授权**：
经统一工作流批准后，在当前租户上下文中将数据应用、服务产品、运行环境和权限范围绑定起来的有效访问许可；租户由上下文自动继承，Agent 和最终用户身份可以作为可选限制条件。
_Avoid_：服务集市可见性、角色

**认证方式**：
数据应用依据服务授权访问服务产品时使用的身份校验方式；默认采用 OAuth 2.0，API Key、JWT 和 mTLS 等作为可选方式，具体可用类型受数据安全域的统一策略约束。
_Avoid_：服务授权、只允许 API Key、由消费者绕过策略任意选择

**认证与密钥策略**：
数据服务域为数据应用对外接入维护的公共认证规则，包括允许与默认认证方式，以及各接入方式对应的凭据有效期和接受边界；必须满足数据安全域底线。OAuth 访问令牌与客户端密钥期限独立，API Key 按凭据期限管理，独立 JWT 接入限制可接受令牌时长，mTLS 接入校验证书有效区间并可限制可接受的总签发周期；设置不能延长已签发的令牌、密钥或证书。类型相关的调用配额与执行限制在独立的“类型专属限制”目录项维护，数据应用在允许范围内形成实际配置，再交由对应执行方落实。
_Avoid_：密钥生命周期管理、独立的数据安全策略、网关运行策略

**类型专属限制**：
数据服务域按七类可调用集市产品维护的调用配额与执行上限，逻辑表不参与；额度以数据应用为主体，其全部凭据共享。原型按调研结果分别表达模型 Token、Agent 执行、Skill 激活与脚本、Worker 任务、API 与 MCP 调用和业务数据流订阅限制。入口控制由统一网关执行，内部执行与订阅限制需要运行时或适配器支持，不视为网关原生通用能力；具体字段和示例值见 `docs/research/settings-type-specific-limits.md`，不代表已确认生产额度或完成后端接入。
_Avoid_：所有类型统一 GB 配额、网关基础设施参数、单凭据独享应用额度

**数据应用凭据**：
外部消费场景调用数据应用中已获授权能力时用于证明身份的凭据；身份粒度与数据应用一一对应，可以采用 OAuth Client、API Key 等认证方式，不得跨数据应用共享。
_Avoid_：服务授权、数据权限、外部系统共享凭据

**API Key**：
数据应用选择 API Key 认证时生成的应用级调用凭证，与数据应用一一对应，只能访问该应用已获授权的能力；支持有效期、禁用和轮换，不允许物理删除。轮换后旧 Key 立即失效，不提供新旧 Key 并行期，历史记录继续保留用于审计。
_Avoid_：跨数据应用共享 Key、按服务重复签发 Key、删除历史凭证、轮换并行期

**服务权限**：
数据应用获准使用某项数据服务的权限集合，由数据权限和操作权限共同组成。
_Avoid_：应用权限、平台功能权限

**数据权限**：
数据服务运行时允许数据应用读取的字段和数据行范围；数据应用是唯一授权主体，权限对应具体数据消费场景，并同时满足来源资产、数据分类分级、敏感数据规则和服务授权约束。服务版本只声明数据依赖和所需范围，不持有运行数据权限；API 来源限定为 ADS 层，数据流来源暂不受数仓语义层级限制。
_Avoid_：服务版本数据权限、服务集市可见性、服务订阅申请

**开发者数据权限**：
绑定数据服务开发者个人、控制其在开发过程中能否发现和引用逻辑表、流式数据资产及其字段和数据范围的权限；权限必须在数据中台发起申请并经审核生效，不得由业务域或数据服务自动继承。
_Avoid_：服务授权、数据应用数据权限、业务域共享权限

**操作权限**：
数据应用获准通过受管服务对中台执行的调用、消费或实质性操作范围；操作权限按明确动作授权，不包含管理平台菜单和后台管理角色。每次执行必须使用数据应用身份并记录审计，高风险或破坏性操作还需要人工确认。
_Avoid_：平台功能权限、数据权限、注册后获得全部操作、管理者个人账号代执行

**统一授权工作流**：
承载服务权限申请、审批、状态触发和过程留痕的平台公共流程；数据服务只使用其能力，不定义工作流引擎内部规则。
_Avoid_：服务授权、数据权限服务

**通用工作流引擎**：
平台复用的流程执行能力，负责上线、下线、服务使用、可见范围变更等申请的流程定义、任务流转、审批人资格和审批状态；创建人与审批人是否隔离及如何隔离均由该引擎的统一策略管理，数据服务平台只发起流程、接收状态和展示待办，不自行固化审批人或职责分离规则。
_Avoid_：审批中心页面、业务操作执行器、数据服务自建流程引擎

**审批中心**：
通用工作流引擎在数据服务平台中的任务入口，集中展示我的待办、我发起的和已处理事项；审批完成后的注册、上线、下线或授权执行结果回到对应业务对象展示。
_Avoid_：流程设计器、业务执行日志中心、独立审批引擎

**运行管理**：
合并统一网关配置控制、服务运监、异常事件和审计能力的管理模块，包含网关概览、运行情况、策略管理、配置发布、服务运监、异常事件和审计日志；导航入口只向管理者展示。运行管理以数据服务能否正常被调用为核心，面向服务流量的期望配置和实际运行状态，主要依据请求量、成功与错误、响应延迟、异常及 AI 降级等运行指标判断健康，不负责 Kubernetes 工作负载、服务网格或网关基础设施运维。
_Avoid_：独立的统一网关一级模块、独立的运维与审计一级模块

**服务运监**：
按数据应用及其固定服务版本检查运行状况的页面，原名称为运行监控。目录按普通客户端、Agent、MCP、Skill、Worker 等主要消费形态组织应用，仅保留分类与应用两层，服务清单及详情在右侧查看；查询仅支持应用或服务名称，入口收纳在“应用目录”标题右端的放大镜，展开时在同一行以输入框替换标题，关闭后恢复标题。应用自身指标与其调用的服务指标分开，服务层再按七类可调用产品选择观测口径。请求、运行、工具调用、脚本、任务及数据流消费不合并成统一成功率或延迟分位；外部运行未接入执行观测时保持未知，不由平台 API 调用成功推断外部执行健康。
_Avoid_：统一请求指标大盘、应用分类等同服务产品类型、基础设施监控、由调用推断执行

**运行可用状态**：
服务实际访问目标能否接受新调用的状态，采用验证中、可用和不可用表达；它独立于服务版本和数据应用版本，运行配置变化可以改变该状态而不产生新版本。
_Avoid_：服务版本、数据应用版本、审核状态

**运行审计**：
对数据服务域内的管理操作、系统运行日志、外部数据调用和中台数据加工过程形成不可修改的结构化留痕，用于回答何时、由何主体、对何对象执行了何事项、使用了哪些授权或数据范围以及结果如何。外部调用审计关联数据应用、外部消费场景、服务固定版本、认证方式、服务授权和数据权限；中台加工审计关联加工任务、来源与目标资产、调度批次、读写数量、耗时和结果。审计只保存必要元数据和受控引用，不保存秘密值、请求与响应正文，也不承载主机、容器或基础设施日志全文。
_Avoid_：仅管理操作审计、访问日志全文、请求或响应正文、基础设施日志中心、可修改审计记录

**配置发布**：
将某个环境中的统一网关期望配置经过校验、差异确认、风险审批和技术执行后变为实际配置的一次有版本记录的变更；失败可以重试，回滚必须以历史快照为目标形成新的配置发布。
_Avoid_：服务版本发布、服务上线、直接修改历史配置

**配置漂移**：
统一网关实际配置与数据服务平台记录的期望配置不一致的状态；平台先展示结构化差异，再由管理者选择通过新的配置发布覆盖实际状态或接纳实际状态，不自动覆盖任一侧。
_Avoid_：配置发布失败、自动覆盖、无审计的网关直改

**紧急停流**：
发生数据泄露、错误路由、后端故障或安全事件时，由管理者立即停止指定生产流量的高风险处置动作；可以绕过普通事前审批，但必须二次确认、完整审计并在事后补充事件和审批关联。
_Avoid_：正常下线、删除服务、无审计停流

**简单权重灰度**：
在同一服务的当前版本与候选版本之间按显式权重分配流量的发布方式；首期不包含基于地域、用户标签或复杂表达式的金丝雀规则。
_Avoid_：全量切换、复杂金丝雀发布

**AI 多模型路由**：
一个对外逻辑模型按权重选择多个物理模型目标，并在目标不可用时按优先级执行降级的统一网关路由能力。
_Avoid_：单模型绑定、按成本或语义自动选模

**ADS 层**：
面向应用消费组织数据资产的数据分层，是 API 服务获准使用的唯一中台数据来源；暂不作为数据流服务来源的硬性层级限制。
_Avoid_：所有流式数据资产所在层、任意数据源

**业务域**：
组织数据服务归属、责任和访问边界的业务划分，同一租户内可以存在多个业务域。
_Avoid_：租户、项目

**管理者**：
负责数据服务治理、授权审批、运行管理和统一网关管理的人类岗位视角。
_Avoid_：平台管理员账号、数据应用

**开发者**：
由平台基座授予开发岗位权限，负责基于获准使用的数据资产创建、测试、发布和维护数据服务的人类岗位视角；具有开发岗位权限的人员可以进入服务开发并在基座授权的对象范围内工作。API 开发只能引用 ADS 资产，数据流开发可以引用任意语义层中已完成治理和授权的流式数据资产。
_Avoid_：数据应用、Agent

**消费者**：
负责发现、申请和使用数据服务，并管理外部系统接入关系的人类岗位视角。
_Avoid_：数据应用、外部系统

**外部数据消费方**：
在中台之外实际获取平台数据或发起获准中台操作的调用主体，可以采用普通客户端、Agent、MCP、Skill 脚本或 Worker 等技术形态；它通过数据应用取得平台身份和治理边界，不成为服务产品。
_Avoid_：消费者岗位、外部注册能力、平台原生产品

**数据应用**：
外部数据消费方在一个外部消费场景中的平台映射，是数据能力组合、开发配置、数据权限、服务授权和运行观测的主体；每个数据应用只归属一个场景，不允许被多个场景共享。数据应用记录消费方的主要技术形态，可以由平台托管或在外部运行，首期由平台人员创建和开发。
_Avoid_：消费应用、外部系统、单一数据服务

**数据应用运行方式**：
数据应用实际执行消费逻辑的位置，分为平台托管和外部运行；两种方式使用相同的数据应用身份、数据权限、服务授权和审计边界。
_Avoid_：服务环境部署、产品可见范围、数据应用消费形态

**数据应用消费形态**：
数据应用执行数据消费逻辑的主要技术形态，例如普通客户端、Agent、MCP、Skill 或 Worker；每个数据应用只选择一种主要消费形态，它描述消费方如何运行，不把数据应用转换为同名服务产品。
_Avoid_：集市产品类型、服务产品类型、数据应用运行方式

**外部消费场景**：
一个外部调用系统或客户端为完成特定消费目的而形成的独立身份与治理边界，与数据应用一一对应；需要独立凭据、配额、撤销或审计的调用方必须使用不同的数据应用。
_Avoid_：外部系统、多个调用方共享身份、数据应用集合

**数据应用版本**：
数据应用的数据权限、运行配置、自动分配的网关策略及固定服务版本引用经过审批和发版后形成的不可变快照；选择新服务版本并重新审核通过后自动替代当前版本，仍满足在线、授权和安全约束的历史已批准快照可以直接回滚。
_Avoid_：服务版本、直接修改生效应用、配置发布

**数据应用上线**：
平台托管数据应用经过开发测试、应用版本与数据权限和服务授权审批后，部署到平台运行环境的组合操作；它与 API 服务上线采用相似的人工检查和执行控制，但不把数据应用发布到服务集市。
_Avoid_：服务产品上线、服务集市发布、创建数据应用

**数据应用停用**：
通过审批控制撤销数据应用凭据、授权和网关策略，并将对象逻辑删除的操作；历史版本、审批、调用和审计记录继续保留。
_Avoid_：物理删除、服务下线、仅隐藏页面

**数据应用注册**：
将外部数据消费方在一个消费场景中的信息、主要技术形态和所选平台服务记录为数据应用映射，并纳入身份、权限、凭据和审计治理的过程；注册不创建独立的外部能力对象，也不等同于服务产品上线或已经取得数据权限。
_Avoid_：外部能力注册、服务产品上线、数据应用上线、自动获得数据权限

**调用身份透传**：
外部 Agent 调用受管 MCP、API 或其他服务时，将其所对应数据应用的受信任身份沿调用链传递到最终数据或操作执行点的机制；每一跳只能收窄权限，不得替换为服务自身或管理者的更高权限。
_Avoid_：共享服务账号、MCP 自有数据权限、管理者身份代调

**复制为服务草稿**：
将数据应用中的自用制品一次性复制为具有全新产品身份的服务草稿；复制必须由人工确认，完成后两者拥有独立版本、测试、审批和生命周期，永不自动同步。
_Avoid_：数据应用直接发布、共享可变制品、自动同步

**应用中心**：
面向外部系统接入关系的管理模块，集中呈现数据应用及其凭证、订阅、授权、配额和调用关系。
_Avoid_：数据应用开发工作台

**Agent**：
能够调用模型或工具完成任务、并可被数据应用装配的非人身份主体；Tools、Memory、Prompt 等内容统一作为 Agent 整体的一部分管理、测试、版本化和审核，不拆成 Agent 内部的独立产品或生命周期。Agent 版本声明同步会话、异步任务或两者兼有的调用模式；代表用户执行时可以携带受信任的最终用户委托身份。
_Avoid_：数据应用、外部系统

**沙箱运行环境**：
按数据应用和运行环境划分、用于 Agent 整体安全执行的逻辑隔离边界；底层算力可以共享，但网络策略、数据权限、凭据、Memory、临时数据和运行观测彼此隔离。
_Avoid_：服务集市产品、生产环境、Kubernetes Namespace

**集成应用**：
由平台组合数据服务、模型和工具后形成的可发布产品；仅在平台实际提供组合与发布能力时使用该术语。
_Avoid_：数据应用、Agent

**统一网关**：
共同承载外部系统通过数据应用访问数据 API、模型、Agent、MCP 和相关能力的南北向流量，并统一执行身份校验、路由、流量控制、安全策略及调用观测的运行边界；API 由其中的 API 网关承载，Agent、MCP、Skill 和 Worker 由 AI 网关承载，两者共享数据应用身份、授权和审计链路。
_Avoid_：彼此独立的 API 网关与 AI 网关、MaaS 中台、服务网格

**网关运行投影**：
同一个平台内部服务版本在 API 网关或 AI 网关中的技术注册记录；多个投影共享服务和版本身份，不形成独立服务产品、授权对象或集市条目。
_Avoid_：服务产品、服务版本、跨网关重复产品

**数据服务开发方式**：
API 服务支持传统开发、低代码开发和 Coding Agent 三种入口，首期均生成 REST/HTTP、JSON 和 OpenAPI 契约，并共用同一种服务草稿、测试、版本、审批、上线和运维流程。传统开发支持普通 SQL、Dynamic SQL 和参数契约，只能引用 ADS 逻辑表；低代码开发首期只支持单张 ADS 逻辑表，通过选择请求条件和返回字段生成服务契约；Coding Agent 根据自然语言需求生成可审查、可修改的配置、SQL、契约和测试内容，只能操作草稿，必须由开发者审查、测试并亲自提交上线。
_Avoid_：三套彼此独立的生命周期、Coding Agent 自动上线、API 引用 ADS 以外的数据

**AI 服务开发方式**：
平台托管 AI 服务在可编辑草稿中采用的工作方式，包括 AI 辅助开发和手工开发；两种方式共用同一个服务草稿、草稿修订和治理生命周期，可以在可编辑草稿中切换，不构成新的服务产品、托管方式或正式服务版本。
_Avoid_：AI 服务托管方式、永久创建类型、独立开发生命周期

**AI 辅助开发**：
开发者可以从自然语言开始，由开发 Agent 生成代码、配置、契约、依赖、测试和文档的变更集，也可以完全手工创建；开发者全部或部分接受后才形成新的草稿修订，AI 不直接修改已发布版本。AI 不代替开发者执行测试、提交审核、审批或上线。
_Avoid_：AI 自动提审、AI 自动审批、AI 自动上线、无需人工审查的生成

**手工开发**：
开发者不使用开发 Agent 生成变更、直接编辑类型专属服务开发制品的工作方式；它与 AI 辅助开发共享同一服务草稿和治理边界，仍由开发者执行测试和提交审核。
_Avoid_：非托管服务、独立服务类型、绕过审核的人工修改

**服务开发制品**：
服务开发产生并由数据服务平台持有的代码、配置、契约、依赖声明、测试、文档和版本内容；MaaS 提供开发 Agent 的运行能力，但不成为服务开发制品的归属方或正式真源。
_Avoid_：MaaS 制品、开发 Agent 会话、外部运行地址

**开发 Agent**：
由 MaaS 中台管理、供服务开发使用的 AI 创作主体；它辅助生成 API、Agent、MCP、Skill 和 Worker 的草稿内容，但不成为这些服务产品的所有者或审批主体。
_Avoid_：数据应用中的 Agent、待开发的 Agent 产品、人类开发者

**MaaS 中台**：
管理开发 Agent 及其线程、提示词、模型中转、费用、并发和用户访问等运行能力的平台；数据服务平台使用其开发 Agent 辅助生产服务内容，全部服务开发制品及其审批记录归属数据服务平台。
_Avoid_：统一网关

**外部系统**：
独立于数据服务平台建设、部署在内网或外网并实际消费数据服务的系统；同一外部系统可以具有多个外部消费场景，但每个场景都通过专属数据应用取得调用身份，不直接成为服务授权主体。
_Avoid_：数据应用

**服务版本**：
数据服务在一次发布后形成的不可变契约和可部署制品；状态依次为草稿、审核中、已批准、已发布、已废弃、已归档，驳回后返回草稿修改。
_Avoid_：环境部署、集市可见状态、服务生命周期状态

**草稿修订**：
AI 变更或人工保存后形成的可恢复草稿历史；它可以被比较、恢复或继续修改，但不是正式服务版本，不能上线或被数据应用选择。
_Avoid_：服务版本、已发布制品、运行版本

**服务依赖**：
服务产品在开发时显式声明并随具体服务版本固定的数据资产、其他服务或执行能力引用；依赖图禁止循环，上线审核检查完整依赖闭包，数据应用选择该服务时必须看见并申请依赖闭包所需的数据与服务授权。
_Avoid_：运行时自动跟随最新版、数据应用手工重建服务内部装配、未声明的隐式依赖

**受管依赖**：
因获取平台数据、形成对外数据通道或对中台执行实质性操作而必须完成平台注册、版本和治理的服务依赖；纯本地、无平台数据、无外部通道且不改变中台状态的辅助能力不因此自动成为服务集市产品。
_Avoid_：所有本地工具强制上架、未注册的数据通道、未治理的中台写操作

**环境部署**：
某个已发布服务版本在指定环境中的运行实例；生命周期为未部署、部署中、运行中、停止中、已停止或部署失败，运行健康度另以正常、降级、异常表达。
_Avoid_：服务版本、集市可见状态

**集市可见状态**：
服务产品在本租户服务集市中的可见状态；生命周期为不可见、生效中、可见、失效中、已失效或执行失败，状态失效后仍保留完整历史记录。
_Avoid_：服务版本发布、环境部署

**操作申请**：
上线、下线等治理操作进入统一授权工作流后形成的业务记录；审批状态与实际执行状态分离，审批通过不代表执行成功。
_Avoid_：服务订阅申请、服务授权

**上线审核**：
数据服务开发者将服务版本发布并部署到生产环境前必须发起的管理审批；由管理者审核服务契约、来源资产、声明的数据依赖及所需范围和生产配置，其中 API 来源必须属于 ADS，数据流来源不按数仓层级限制但必须完成治理。上线审核不向服务版本授予数据权限；数据权限只在数据应用使用服务时审批。
_Avoid_：开发者自行上线、执行成功

**上线**：
面向开发者和管理者呈现的统一业务操作；管理者审核通过后，系统依次完成服务版本发布、生产环境部署和集市可见状态生效。三个内部步骤分别保留状态和审计记录，但不要求用户分开操作。
_Avoid_：仅发布版本、仅部署环境、仅改变集市可见状态

**下线**：
面向开发者和管理者呈现的停止服务业务操作；即使仍有数据应用引用，完成影响确认后也允许直接执行，并停止新调用。集市可见、授权、受影响应用和执行记录均保留，不通过物理删除表达下线。
_Avoid_：仅改变集市可见状态、仅停止部署、删除服务

**开发仿真测试**：
使用模拟、仿真或脱敏数据验证服务契约、权限和调用轨迹的开发测试方式；写入和实质操作只返回预计变更，不读取生产明文数据或改变真实中台状态。
_Avoid_：生产验证、真实写入、开发环境直连生产数据

**消费调用**：
外部系统通过数据应用中的 API、Agent 等已获授权入口使用集市产品；具体入口受产品类型、统一网关策略和服务授权约束。
_Avoid_：服务集市可见、服务订阅申请、开发者引用逻辑表

**服务生命周期状态**：
与用户执行的上线、下线操作直接对应的主状态，包括草稿、待上线审核、上线中、已上线、待下线审核、下线中和已下线，并包含驳回、取消和执行失败等异常状态；由操作申请及内部执行结果计算，不允许人工修改。
_Avoid_：服务版本状态、环境部署状态、集市可见状态

## Design Constraints

- 运行管理的配置页面以本地 HiMarket 项目 `/mnt/d/cursor/himarket` 为主要产品交互参考，以 Higress 项目 `/mnt/d/cursor/higress` 补充网关配置细节，选择性吸收其信息架构、字段组织、状态和交互模式；设计保持轻量，当前不承诺复用代码、部署源项目或形成运行时依赖。
- 两个参考项目中的品牌、Logo、厂商专属对象、原始基础设施参数和实现细节不得进入生成界面；具体网关实现继续通过厂商中立的适配边界接入。
- DataLeap API 开发仅作为传统开发和低代码交互参考；本平台必须额外提供 Coding Agent 开发入口。
- API 服务只能引用 ADS 层资产；数据流服务暂不设置 ADS 层级限制，但只能引用已登记、受治理并取得数据权限的流式数据资产。
- 完整平台中，数据服务域作为一级产品域与数据治理、数据安全等一级产品域并列；本仓库原型只展开数据服务域。域内左侧二级导航采用工作台、服务开发、服务集市、数据应用、应用与授权、审批中心、运行管理、设置，各二级模块可以按需展开三级目录；服务开发下区分 API 开发与 AI 服务开发。
- 平台原生服务开发独立于数据应用；外部数据消费方通过数据应用注册形成平台映射，不另建外部能力产品、版本、审核或生命周期对象。平台内部 API、Agent、MCP、Skill、Worker 独立上线并注册到网关后，才可由数据应用选择固定版本。
- API、Agent、MCP、Skill 和 Worker 首期支持平台原生开发；模型只通过 AI 网关注册管理，作为外部数据消费合法合规治理的对象，不进入首期原生开发。AI 可以生成或修改草稿、代码、契约、测试和文档等内容，但不能代替开发者执行测试、提交审核、审批或上线。
- AI 服务创建时明确选择是否平台托管。平台承担托管原生服务的开发与实际运行，不把托管产物仅作为外部地址登记；非托管分支只登记数据需求，不创建外部能力产品、运行配置或可部署服务版本。
- 所有 AI 服务详情均提供数据需求选择与配置，包含资产、字段、行范围和用途；未使用平台数据时显式声明。两种托管方式均需人工提交并进入审核流程；审核中冻结提交快照，驳回后允许修改重提。非托管需求审核通过不触发部署或授权，托管服务审核通过也不绕过发布和数据应用授权边界。
- AI 服务在草稿配置（含已驳回）或仿真测试阶段可经二次确认从服务目录删除；待审核、审核通过及已上线阶段不开放删除。已有审核通过或上线历史的服务不因创建新草稿而获得整项删除能力，既有版本和历史记录不得被连带清除。离线原型采用本地删除标记，不执行后台物理删除。
- 托管服务开发共用服务信息、数据需求、AI 助手、版本、测试和提审外壳，API、Agent、MCP、Skill、Worker 分别使用符合自身制品和契约的专属编辑器，不使用一套通用聊天框或配置表单替代类型差异。AI 服务非托管分支不展示代码、AI 开发助手、测试或部署入口。
- Agent 原生开发形成整体制品，统一承载运行代码或配置、输入输出契约、依赖、Tools、Memory、Prompt、测试和文档并整体版本化；不强制采用可视化节点工作流，也不退化为只登记外部地址。
- MCP 原生开发形成遵循 MCP 协议的可托管服务制品；协议契约是开发、测试、版本和审核的共同真源，外部地址登记只属于注册路径。
- Skill 原生开发形成版本化能力包，可以包含说明、脚本、资源、依赖和测试；既可装配到平台 Agent 沙箱，也可按固定版本交付外部 Agent。外部 Agent 若借此获取平台数据、形成外部数据通道或执行中台实质操作，仍必须映射到已注册的数据应用身份。
- Worker 原生开发形成独立的可部署执行制品，表达任务输入输出、触发、超时、重试、资源限制、依赖、测试和运行清单；Worker 不等同于模板下载或 Agent 运行模型。
- Worker 首期支持由 Agent 或 API 按需提交异步任务和平台定时触发，不承诺事件流触发或复杂任务编排。
- Agent 服务版本必须明确声明同步会话、异步任务或两者兼有的调用契约，数据应用只能按已批准的固定版本契约调用。
- 提交上线前保留人工确认和基本可审核性要求，但本次不继续细化跨类型公共质量门禁、阈值和自动阻断规则；类型专属协议与必要测试仍属于对应开发契约。
- 开发 Agent 及其线程、提示词、模型中转、费用、并发和用户访问由 MaaS 中台管理；数据服务平台负责向开发 Agent 提供受控上下文，并作为全部代码、配置、契约、依赖声明、测试、文档、版本和审批记录的唯一正式真源。
- 服务开发和数据应用注册复用平台基座提供的统一角色权限与审计服务，不在数据服务域内另建账号、角色体系或审计底座；具备开发岗位权限的人员可以使用平台原生服务开发，具体对象范围继续由基座授权控制。
- 开发 Agent 只能读取当前服务草稿、当前开发者已获授权的数据资产元数据、已批准可引用的依赖，以及平台提供的受控样例和规范；不得绕过开发者权限读取生产数据、未授权资产、其他服务草稿或秘密值。AI 的每次内容修改均保存为可追溯的草稿修订并纳入版本管理。
- 服务开发时按类型声明依赖并随服务版本固定：API 依赖 ADS 数据资产；MCP 可以依赖数据资产和 API；Agent 可以依赖模型、API、MCP 和 Skill；Skill 可以依赖 API、MCP、脚本和数据资产；Worker 可以依赖数据资产、模型、API、MCP 和 Skill。CLI 暂不纳入首期依赖类型，模型服务暂不定义下游服务依赖。全部依赖禁止循环，上线审核检查依赖闭包，数据应用申请服务时一并展示并审批依赖闭包所需的数据与服务授权。
- 管理者在数据应用创建与维护过程中登记外部数据消费方、主要消费形态、来源标识、运行位置、契约、认证引用、所选平台服务、数据通道、中台操作、责任、测试和合规信息；秘密值不得以明文进入应用记录，外部用户当前不自助办理。
- 服务上线审核和数据应用审核的审批人资格及创建人与审批人隔离策略统一交由通用工作流引擎管理，数据服务平台不重复定义固定人员或硬编码隔离规则。
- 数据应用的运行方式与复用目的分开建模：平台托管和外部运行不改变其消费身份；仅供当前场景使用的制品保留为数据应用运行内容，原则上不复用、不转换为共享服务产品。确需向其他数据应用提供能力时，应独立创建或注册服务产品并进入相应上线和集市流程，不允许数据应用对象本身直接发布到服务集市。
- 数据权限只授予数据应用，不授予服务产品或服务版本。服务开发和上线只声明、校验数据依赖及所需范围；数据应用选择服务后，以具体消费场景申请字段和行范围，运行时始终使用数据应用获批的数据权限。
- 平台托管的 Agent 必须作为包含 Tools、Memory、Prompt 等内容的整体在数据应用沙箱内运行；这些内部内容不拆成独立产品或生命周期。外部运行的数据应用仍受相同身份、数据权限、服务授权、配额和审计约束。
- 每个数据应用只声明一种主要消费形态；一个场景需要多个独立运行身份、凭据、配额或故障边界时，应拆分为多个数据应用。
- CLI 作为底层组件操作的潜在封装方式保留为后续能力，首期不把 CLI 建模为依赖类型、服务集市产品或任意命令入口。
- 模型仍可由 AI 网关承担注册与合法合规治理，但本次功能不继续设计模型的原生开发、具体上线状态和集市交互。
- 外部 Agent 装配或调用涉及平台数据获取、对外数据通道或中台实质操作的 MCP 时，该 MCP 必须先在 AI 网关完成注册；注册只建立 MCP 的治理与使用资格，具体数据应用仍需按其固定版本申请服务授权。
- Agent 以整体制品提交测试和上线审核，不为 Tools、Memory、Prompt 分别创建审批对象；审核详情仍必须展开 Agent 的数据资产、模型、API、MCP、Skill 依赖，所需数据范围、敏感操作、Memory 策略和沙箱权限。
- 平台托管数据应用采用独立于服务产品的上线流程：草稿经过开发测试后提交应用上线审核，审核数据权限、服务授权、固定能力版本、运行配置和沙箱安全；审批通过后部署并进入运行状态。该流程可以复用 API 上线的检查与执行模式，但数据应用不进入服务集市。
- 依赖注册采用风险触发边界：获取平台数据、形成对外数据通道或对中台执行实质性操作，任一条件成立即必须成为受管依赖；纯本地、无平台数据、无外联且不改变中台状态的辅助能力可以保留在数据应用或服务制品内部，但仍受版本、沙箱和审计约束。
- 外部运行的数据消费场景通过数据应用注册纳入治理；注册成功只形成数据应用身份，后续仍需申请数据权限和服务授权并完成凭据及网关策略下发。
- 所有向外部提供平台数据，或允许外部主体对中台执行直接实质操作的对象，都必须通过统一网关中的 API 网关或 AI 网关完成注册、路由、认证、授权、策略和审计治理，不允许建立绕过网关的直接数据或操作通道。
- 外部数据消费方以普通客户端、Agent、MCP、Skill 脚本或 Worker 等主要形态映射为数据应用；所选平台内部服务已经独立完成网关注册，但只有数据应用整体审核、数据与服务及操作授权、凭据和策略下发全部成功后，消费方才可使用。
- 外部 Agent 必须先注册为 Agent 形态的数据应用；调用受管 MCP、API 或其他服务时透传该数据应用身份，服务自身不得持有或替换为更高数据权限，调用链每一跳只能收窄获批范围。
- 数据应用注册与授权分离：注册审核确认消费主体、目的、运行方式和主要消费形态；注册通过后分别申请数据权限、服务授权和必要的操作权限，全部批准并完成凭据及网关策略下发后才进入已生效状态。
- 数据应用自用制品原则上不复用；确有必要时允许人工执行一次“复制为服务草稿”，新草稿具有独立产品身份、版本、测试、审批和生命周期，复制后永不与原数据应用制品自动同步。
- 对中台执行实质性操作的能力必须先成为受管依赖，数据应用再按动作申请操作权限；每次调用透传应用身份并审计，高风险或破坏性操作增加人工确认，不允许使用管理者个人账号作为运行凭据。
- 运行管理二级入口只向管理者展示；开发者和消费者在其有权限的数据服务详情、数据应用详情及工作台中查看自身范围内的运行数据。
- 运行管理固定展开网关概览、运行情况、策略管理、配置发布、服务运监、异常事件、审计日志七个三级目录；首版只显示只读当前环境，不提供环境切换。
- 运行情况提供默认“总览”以及网关、注册中心、AI 治理中心、沙箱四类逻辑实例目录；选择实例后所有运行内容使用同一实例范围。网关展示请求与服务访问；注册中心展示服务、健康目标、订阅与推送；AI 治理中心按参考实现表达 Skill 资源版本与分发；沙箱表达数据应用隔离范围内的执行状态、结果、超时与耗时，不扩展为基础设施运维。总览保留网关请求汇总与各类型独立摘要，不混合不同计量对象。网关概览中的实例表、右侧详情和跳转与该目录共用命名与分类；示例指标及原生/适配器派生边界见 `docs/research/runtime-instance-metrics.md`，不代表完成采集接入。
- 进入运行管理时默认打开网关概览；网关概览承担当前环境的整体运行与配置状态摘要，不替代各专项三级页面。
- 异常事件保留可操作的简化表格页；审计日志以管理操作、系统运行日志、外部数据调用和中台数据加工四类结构化记录提供基础筛选与详情。两者均不设计告警规则、通知、日志全文检索或审计策略。
- 数据应用首期只允许平台人员创建和开发，不向外部用户提供申请创建通道；每个数据应用只服务一个外部消费场景，场景之间不得共享数据应用或调用凭据。
- 数据应用增加、移除或调整数据权限及 API、Agent、MCP、模型、Skill、Worker 等能力时，必须形成受版本控制的变更，并经过申请、审批和发版后生效，不允许直接修改当前生效版本。
- 数据应用版本采用一次发版申请、授权项分别审批、版本整体生效；任何必需授权项未通过时，整个版本返回开发状态，不允许部分配置进入运行版本。
- 当前数据应用原型只表达同一环境中的开发版本和运行版本，不设计环境切换；环境部署仍是服务产品自身的独立运行概念。
- Agent 与模型的沙箱按“数据应用 × 运行环境”形成逻辑隔离；沙箱不是服务集市产品，底层资源实现属于外部运行基础设施。
- 数据分类分级和敏感数据规则作为数据权限审批与运行校验的下层约束存在；当前原型不设计其管理页面或细化判定逻辑，只将其视为外部数据治理约束。
- 设置是数据服务域内的二级功能，由管理者维护并服务于域内各业务功能，只承载该域特有、跨服务复用、确需配置且不更适合归属其他模块的管理对象或规则；系统设置、基础设施运维、运行状态和仅供查阅的外部消费方摘要不进入设置。当前采用目录与详情页面，包含集市产品类型、认证与密钥策略、类型专属限制三个独立入口；租户信息、逻辑环境生命周期和网关适配器不由设置管理。
- 集市产品类型采用平台支持目录；设置中的“新增”和“删除”分别表达启用和停用对外呈现，不创建或物理删除类型。停用只隐藏服务集市中的类型及其新发现入口，不阻止创建或注册，不改变既有产品、授权和调用；单个产品的下线继续遵循自身生命周期。
- 集市产品类型只允许维护展示名称、说明、排序和对外呈现状态；类型的字段、表单和处理逻辑由平台固定。逻辑表继续作为集市资产类型参与呈现管理，但明确标记为不可直接调用。
- 集市产品类型的展示名称、说明和排序保存后立即生效并记录审计；停用对外呈现前必须展示受影响产品数量并二次确认，不进入审批流程。
- 数据安全域规定认证和密钥安全底线；数据服务域设置维护公共认证规则与类型专属上限；每个数据应用在允许范围内形成实际配置；统一网关、运行时和订阅适配器按职责执行其运行投影，运行管理不重复编辑业务配置。
- 类型专属限制中的数据流量指单位周期内允许传输的数据总量，不编辑实时网络带宽，也不替代模型 Token 或执行资源计量；并发和周期额度均以数据应用为主体，其所有凭据共享额度。
- 认证方式及对应的凭据有效期、接受边界采用公共策略；允许方式与各自配置联动，默认方式只能从已启用方式中选择。OAuth、JWT、mTLS 在此表达接入配置档案，不表示底层协议互斥。七类可调用产品在独立目录按能力表达专属限制，逻辑表不参与。设置修改本身只进行影响预览、必要的二次确认和审计，不另行发起审批；数据应用的实际配置变更继续通过应用版本审批与发版生效。原型中需要认证服务、运行时或适配器配合的字段不等于网关已支持，必须保留执行方与待适配边界；具体契约及示例值见 `docs/research/settings-type-specific-limits.md`。
- 收紧公共认证策略或类型专属限制时，如果任何运行中的数据应用会超出新限制，系统必须阻止保存并列出受影响应用；管理者应先通过数据应用新版本完成调整。放宽限制或不影响既有应用的修改可以保存；数据安全域因安全事件实施的紧急强制限制走独立治理路径。
- 环境是开发、测试、生产等逻辑运行环境；网关适配器以厂商中立的实例连接、能力声明和引用对接具体实现。Kubernetes 集群、Namespace、Pod、CRD、服务网格、密钥原文和可观测基础设施由外部基础设施系统管理。
- 生产网关配置变更按风险分级：高风险变更进入审批，低风险变更在校验通过后可以自动执行；所有变更均形成配置发布和审计记录。
- 首期支持简单权重灰度，以及 AI 逻辑模型到多个物理模型目标的权重路由和优先级降级；复杂金丝雀和成本、延迟、语义选模暂不承诺。
- Prompt 和响应正文默认不采样；只有指定服务和环境经过单独审批并完成脱敏后才可限时采样。WAF 只管理外部策略引用、绑定范围和生效状态，不编辑规则。
- 审批中心采用我的待办、我发起的、已处理三个固定三级菜单；抄送我的仅在通用工作流引擎支持时显示。
- 审批任务在数据服务平台内展示统一审批详情，并通过通用工作流引擎 API 完成批准、驳回或转交等操作，不跳转到外部流程页面。

## State Models

### 服务生命周期主状态

`草稿 → 待上线审核 → 上线中 → 已上线 → 待下线审核 → 下线中 → 已下线`。

该主状态机适用于 Agent、MCP、模型、Skill、Worker、API 和数据流。逻辑表不进入服务生命周期主状态机，只管理集市可见状态和开发者数据权限。

- 待上线审核可以进入已驳回或已取消；已驳回后返回草稿修改。
- 上线中可以进入上线失败；必须展示内部失败步骤并允许重试。
- 待下线审核可以进入已驳回或已取消；服务仍保持已上线。
- 下线中可以进入下线失败；必须展示内部失败步骤并允许重试。
- 已下线服务可以重新进入待上线审核；上线成功后自动恢复仍在有效期内且未被撤销的原服务授权，已过期或已撤销授权不得恢复。
- 主状态必须与上线、下线操作申请及内部执行状态对应，不允许人工直接修改。

### 服务版本

`草稿 → 审核中 → 已批准 → 已发布 → 已废弃 → 已归档`；审核驳回后进入已驳回并返回草稿修改。已发布版本不可直接修改，变更必须创建新版本。

### 环境部署

`未部署 → 部署中 → 运行中 → 停止中 → 已停止`；部署过程可以进入部署失败。运行中实例的健康度独立表达为正常、降级或异常。

### 集市可见状态

`不可见 → 生效中 → 可见 → 失效中 → 已失效`；集市可见状态变更过程可以进入执行失败，每个服务产品分别保存状态和完整历史。

### 操作申请

`草稿 → 待审批 → 已批准 → 执行中 → 执行成功`；可以进入已驳回、已取消或执行失败。上线、下线分别生成操作申请。

数据服务上线必须由开发者发起申请并经管理者审核。审核通过后系统自动执行版本发布、生产部署和集市可见状态生效；审批通过不等于三个内部步骤均已执行成功。

### 服务授权

`待审批 → 待下发 → 已生效`；可以进入已拒绝、下发失败、已暂停、已过期或已撤销。授权主体为数据应用，可选关联 Agent 和最终用户委托身份。

### 数据应用生命周期

`草稿 → 开发测试中 → 待应用上线审核 → 部署中 → 运行中 → 待应用下线审核 → 下线中 → 已下线`。开发测试或审核驳回后返回草稿；部署与下线可以进入执行失败。该状态机只适用于平台托管数据应用，不改变数据应用作为消费主体的身份，也不产生服务集市可见状态。

### 外部数据应用注册

`草稿 → 待注册审核 → 已注册 → 待授权 → 策略下发中 → 已生效`；注册审核可以进入已驳回或已取消，策略下发可以进入下发失败。已注册只代表形成数据应用身份，必须在数据权限、服务授权和必要操作权限全部批准且凭据与网关策略下发成功后才可生效。

### 配置发布

`草稿 → 校验中 → 待审批或待发布 → 发布中 → 已生效`；可以进入校验失败、已驳回、已取消、部分失败或发布失败。高风险生产变更进入待审批，低风险或非生产变更校验通过后进入待发布。失败可以重试；回滚以历史快照为目标创建新的配置发布，不修改既有记录。

### 标准动作语义

- 上线：面向用户的组合操作，内部依次执行版本发布、生产部署和集市可见状态生效。
- 发布：形成不可变、可部署的服务版本，不改变服务集市可见性和现有调用。
- 下线：面向用户的组合操作，内部依次执行集市可见状态失效、暂停已有授权和停止生产流量，会影响现有调用。

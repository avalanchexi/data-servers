# 统一数据应用目录展示

Type: prototype
Status: resolved
Blocked by: 03

## Scope

按用户浏览器批注，将数据应用列表迁移到服务集市的目录与工具栏风格：只保留模糊搜索，增加卡片／列表切换，将主操作改为“新增”；列表移除业务域、关联 API 两列和版本入口。保留数据应用状态、外部消费场景、最近发版以及查看、编辑、发布、下线、删除等既有业务行为。

## Answer

- 数据应用目录已改为与服务集市一致的标题、结果数、搜索、视图切换和主操作布局，默认使用卡片视图。
- 搜索支持按空格拆分的多关键词模糊匹配，覆盖应用名称、外部消费场景、应用编号和描述；业务域与状态筛选已从界面移除。
- “新建数据应用”主操作改为“新增”，点击后仍进入原有三步新建流程。
- 列表仅保留数据应用、外部消费场景、运行状态、最近发版和操作五列；移除业务域、关联 API 以及“版本”操作。
- 1348×992 与 1440×900 下，数据应用面板、工具栏和列表表格均满足 `clientWidth === scrollWidth`，无横向溢出。
- 内联脚本语法、设计系统校验、HTTP 200、卡片／列表切换、多关键词搜索、新增入口和 Playwright 控制台检查均通过。

## Comments

- 2026-09-01：卡片视图默认展示；搜索匹配应用名称、外部消费场景、应用编号和描述。
- 2026-09-01：本轮不调整数据应用的新建流程与详情页信息架构。
- 2026-09-01：验收期间检测到并行更新；保留其数据应用标题与卡片信息区，只重新核验本轮字段、搜索、视图切换和操作边界；最终落盘版本已同步移除刷新控件及其遗留监听。

---
status: accepted
---

# 数据应用映射外部数据消费方

已有的外部 Agent、MCP、Skill、Worker 或普通客户端接入平台时，不再创建独立的外部能力产品、版本或审核对象，而是以数据应用映射一个外部数据消费方的具体消费场景，并在应用版本中记录其主要技术形态、所选内部服务和授权；平台内部服务仍独立开发、上线和注册网关。该边界牺牲了对外部运行代码建立独立产品生命周期的能力，但避免把消费主体误建模为可复用服务，并使身份、权限、凭据、版本和审计始终收敛到数据应用。

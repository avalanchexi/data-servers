## Agent skills

### Issue tracker

议题使用本地 Markdown 文件管理，存放在 `.scratch/<feature>/` 下。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用五个默认的标准分流标签，标签名保持不变。详见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用单上下文领域文档布局。详见 `docs/agents/domain.md`。
